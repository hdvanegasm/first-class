<ul>
  {{#each model.people as |person|}}
    <li class="salutate">
      {{#link-to 'people.person' person.id}}
        Hello {{person.name}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>
<li class="salutate">
  <a href="#" {{action 'incrementSalary'}}>Increment</a>
</li>

{{outlet}}

<p><strong>Total salary: {{totalSalary}}</strong></p>
